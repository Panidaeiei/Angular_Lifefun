export interface Comment {
    post_id: number;
    title: string;
    uid: number;
    date_time: string;
    notify: number;
    username: string;    // เพิ่ม username
    image_url: string;   // เพิ่ม image_url
  }
  
  