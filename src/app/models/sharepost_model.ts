export interface SharePostModel {
    post_id: number;    
    uid?: number;        
    date_time?: string;  
    notify?: number;    
    isShared?: boolean;
    hasMultipleMedia?: boolean;   
  }
  